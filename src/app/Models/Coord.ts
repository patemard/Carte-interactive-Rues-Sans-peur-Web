export class Coord {
  latitude: number;
  longitude: number;

  constructor(lattitude?: number, longitude?: number) {
    this.latitude = lattitude || 0;
    this.longitude = longitude || 0;

  }


}

